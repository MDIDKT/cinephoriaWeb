{% extends 'base.html.twig' %}

{% block body %}
    <h1>Éditer le film {{ film.titre }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_films_index') }}">Retour à la liste</a>
{% endblock %}